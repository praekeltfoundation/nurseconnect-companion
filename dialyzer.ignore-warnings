### These are from the prometheus ecto instrumenter. Since it's an external package, not much we can do about it
Function microseconds_time/1 has no local return
The call 'Elixir.System':convert_time_unit(_@1::any(),'native','micro_seconds') breaks the contract (integer(),time_unit() | 'native',time_unit() | 'native') -> integer()
